# Client calling the Indexer API must provide this token in the Authorization header
app:
  http:
    auth-token: Q*z6OfL^4u^1nMhH

server:
  port: 8081

elasticsearch:
  portal_index:
    serverUrl: http://3.27.134.253:9200
    apiKey: cEFDM3E0b0JJMG9OZ1R4YzlCR1Q6ZGxsQnBvRXlTcm12SFVaYjFIM24zdw==

geonetwork:
  search:
    api:
      endpoint: http://localhost:8080/geonetwork/srv/api/search/records/_search

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    disable-swagger-default-url: true

logging:
  level:
    au.org.aodn.esindexer.service: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
